--- a/src/widgets/mindmap_widget.tsx
+++ b/src/widgets/mindmap_widget.tsx
@@ -1041,6 +1041,25 @@ function layoutForestHorizontal(
   );
 }
 
+
+/** Collapse first-sphere parent and child rem nodes (forest roots with further hierarchy). */
+function makeDefaultView(
+  collapsed: Set<string>,
+  ancestors: HierarchyNode[],
+  descendants: HierarchyNode[]
+): void {
+  for (const node of ancestors) {
+    if (node.children?.length) {
+      collapsed.add(node.id);
+    }
+  }
+  for (const node of descendants) {
+    if (node.children?.length) {
+      collapsed.add(node.id);
+    }
+  }
+}
+
 function findNodeById(forest: HierarchyNode[], id: string): HierarchyNode | null {
   const stack: HierarchyNode[] = [...forest];
   while (stack.length > 0) {
@@ -3521,6 +3540,8 @@ function MindmapWidget() {
         }
         console.log(`[perf] phase 4 buildVirtualAttributeData: ${(performance.now() - t3).toFixed(0)}ms`);
 
+        makeDefaultView(collapsed, ancestorTreesResult, descendantTreesResult);
+
         const hidden = new Set<string>();
 
         setAncestorTrees(ancestorTreesResult);
