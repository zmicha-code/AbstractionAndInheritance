# Mindmap modules

Extracted from `src/widgets/mindmap_widget.tsx`.

- `types.ts` — shared types
- `timeout.ts` — load timeout helpers
- `constants.ts` — spacing, handles, colors
- `cache.ts` — load context and Rem caches
- `state.ts` — persisted widget state
- `flowNodes.tsx` — React Flow node components
- `hierarchy.ts` — ancestor/descendant trees
- `remLayout.ts` — rem forest layout
- `attributeData.ts` — property/interface data
- `attributeTrees.ts` — attribute tree layout
- `attributeIntegrate.ts` — integrate attributes into the graph
- `attributeLayout.ts` — barrel
- `virtualData.ts` — virtual attribute computation
- `virtualDescendants.ts` — virtual child layout
- `virtualAttributes.ts` — virtual attribute layout
- `virtualLayout.ts` — barrel
- `graph.ts` — graph assembly
